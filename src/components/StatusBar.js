import React from 'react';
import PropTypes from 'prop-types';

const StatusBar = () => {
  return (
    <div className="status">
      Status
    </div>
  );
};

StatusBar.propTypes = {

};

export default StatusBar;
