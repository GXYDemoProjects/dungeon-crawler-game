const occupiedSpaces = (state = {}, action) => {
  switch (action.type) {
    case 'SET_VISIBILITY_FILTER':
      return state;
    default:
      return state;
  }
};

export default occupiedSpaces;
